// import { IUser } from "../interfaces";

// Here to add more Entity interfaces to support all types of documents
export type TModel = any;

export type TModelId = string | Array<string>;

export type RowId = {
    id: number;
};
